# Presidio PII detection service.
# Uses a combined image that includes both analyzer and anonymizer with German language support.
#
# Routes:
#   POST presidio:80/analyze
#   POST presidio:80/anonymize
---
apiVersion: v1
kind: Namespace
metadata:
  name: presidio
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: presidio
  namespace: presidio
  labels:
    app: presidio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: presidio
  template:
    metadata:
      labels:
        app: presidio
    spec:
      containers:
        - name: presidio
          image: ghcr.io/agentic-layer/presidio:0.1.0
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: presidio
  namespace: presidio
spec:
  selector:
    app: presidio
  ports:
    - port: 80
      targetPort: 8000
